#include <bits/stdc++.h>

using namespace std;

int main() {
  int a,b;
  scanf("%d %d", &a, &b);
  char pie[16];

  if (a > 8 || b > 8) {
    printf(":(\n");
  } else {
    printf("Yay!\n");
  }

  return EXIT_SUCCESS;
}
